# Public Demo Workspace

This `.agent-hub` directory is a minimal, synthetic workspace bundled only so the archived application can start without accessing another project.

It contains no real project data, credentials, Provider responses, local paths, or private run history.
